# FastAPI Scalable Scaffold

Minimal, opinionated FastAPI scaffold (production-ready starting point).

Run locally:
```bash
python -m venv venv
venv\Scripts\activate

cp .env.example .env
pip install -r requirements.txt
uvicorn app.main:app --reload
```

Docker + Compose included.

